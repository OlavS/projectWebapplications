@model Model.ViewModels.UserVM
@{
    ViewBag.Title = "EndrePassord";
}

@using (Html.BeginForm())
{
    @Html.AntiForgeryToken()
    <h2>Hva er epost adressen din?</h2>
    <hr />
    <div class="form-horizontal">
        @Html.ValidationSummary(true, "", new { @class = "text-danger" })
        <div class="row">
            <div class="col-lg-4 col-md-6 col-sm-10 col-12">
                <div class="form-group">

                    <div>
                        @Html.LabelFor(model => model.Email, htmlAttributes: new { @class = "", id = "" })
                        @Html.EditorFor(model => model.Email, new { htmlAttributes = new { @class = "form-control form-control-sm", @autofocus = "autofocus", id = "" } })
                        @Html.ValidationMessageFor(model => model.Email, "", new { @class = "text-danger", id = "epostmelding" })
                    </div>
                </div>
            </div>
        </div>
    </div>
    <div class="form-group">
        <div>
            <input id="registrerKnapp" type="submit" value="finn bruker" class="btn btn-dark" />
        </div>
    </div>

}